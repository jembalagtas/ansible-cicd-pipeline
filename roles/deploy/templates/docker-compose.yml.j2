version: '2'
services:
  tomcat:
    container_name: "tomcat"
    build: "/docker/tomcat"
    image: "tomcat-petclinic-{{ build_id }}:latest"
    restart: always
    privileged: true
    ports:
      - "8888:8080"